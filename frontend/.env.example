# Optional: set in production when the API is deployed elsewhere (e.g. Railway, Render).
# Leave unset for local dev (Vite proxies /api to the Flask server).
# VITE_API_URL=https://your-api.example.com
