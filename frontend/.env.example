# Optional: set when the API is on a different origin. Must include /api path.
# Leave unset for local dev (Vite proxies /api to the Flask server).
# Local (if not using proxy): VITE_API_URL=http://127.0.0.1:5002/api
# Production:                VITE_API_URL=https://your-api.example.com/api
